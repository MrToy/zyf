version: '2'
services:
    web:
        build: .
    mongo:
        image: mongo
    proxy:
        image: nginx
        ports:
            - 80:80
        volumes:
            - ./nginx.conf:/etc/nginx/conf.d/default.conf
            - ./dist:/usr/share/nginx/html

web:
  image: daocloud.io/qq08420/zyf
  links:
    - mongo:mongo
  volumes:
    - /data/db:/data/db
mongo:
    image: mongo
proxy:
    image: nginx
    ports:
        - 80:80
    volumes:
        - /home/nginx.conf:/etc/nginx/conf.d/default.conf
        - /home/dist:/usr/share/nginx/html
